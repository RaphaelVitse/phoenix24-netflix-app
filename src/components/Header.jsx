import logo from "./img/logo-netflix.png";

const Header = () => {
  return (
    <header>
      <div>
        <img src={logo} alt="logo neflix" />
      </div>
    </header>
  );
};

export default Header;
